"""The HalveIt test suite.

Standard library only, so it runs on whatever Python is already installed.

    python3 -m tests             everything
    python3 -m tests --fast      skip anything that needs ffmpeg
"""
